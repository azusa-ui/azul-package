% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/azul_survtable.R
\name{azul_survtable}
\alias{azul_survtable}
\title{Publication-ready parametric survival table (PH and AFT)}
\usage{
azul_survtable(
  model,
  exponentiate = TRUE,
  digits = 2,
  outcome = NULL,
  font = "Helvetica",
  font_size = 10
)
}
\arguments{
\item{model}{A \code{\link[survival:survreg]{survival::survreg()}} object fitted with \code{dist} one of
\code{"exponential"}, \code{"weibull"}, \code{"loglogistic"} or \code{"lognormal"} (AFT-only).}

\item{exponentiate}{Logical. If \code{TRUE} (default) estimates, confidence
intervals and standard errors are shown on the ratio scale (HR/OR, TR). If
\code{FALSE} they are shown on the log scale (log(hazard)/log(odds), log(time)).}

\item{digits}{Number of decimal places for \strong{every} value column —
estimates, confidence intervals, standard errors and the statistic.
Default \code{2}. The p-value column is \emph{not} affected: it is always shown to 3
decimal places, and values below 0.001 are printed as \verb{<0.001}.}

\item{outcome}{Optional character label for the outcome, used in the table
caption. If \code{NULL} (default) the response (time) variable name is parsed
from the model formula.}

\item{font}{Font family for the whole table (caption, header, body and
footer share it). Default \code{"Helvetica"}.}

\item{font_size}{Font size in points. Default \code{10}.}
}
\value{
A \code{\link[flextable:flextable]{flextable::flextable()}} object that renders in both HTML and Word.
}
\description{
Build an APA-styled table from a parametric survival model fitted with
\code{\link[survival:survreg]{survival::survreg()}}. Both the proportional-hazard (PH) metric and the
accelerated-failure-time (AFT) metric are presented side by side, with 95\%
confidence intervals, standard errors, the Wald statistic and p-values.
}
\details{
The fitted distribution is detected automatically from the model:
\itemize{
\item \strong{exponential} and \strong{Weibull} models have a proportional-hazards form, so
the left block is labelled \emph{Proportional Hazard (PH)} and the estimate is a
hazard ratio (HR);
\item the \strong{log-logistic} model has no proportional-hazards form (it is a
proportional-odds model), so the left block is labelled
\emph{Proportional Odds (PO)} and the estimate is a survival odds ratio (OR).
}

The right block is always the \emph{Accelerated Failure Time (AFT)} metric, whose
estimate is a time ratio (TR). The AFT estimates are taken directly from
\code{survreg}; the PH/PO estimates are obtained from the AFT coefficients by the
delta method (\eqn{-\beta/\sigma}), reproducing
\code{SurvRegCensCov::ConvertWeibull()} and \code{flexsurv} exactly.
}
\examples{
library(survival)
m <- survreg(Surv(time, status) ~ sex + ph.ecog, data = lung, dist = "weibull")
azul_survtable(m)
azul_survtable(m, exponentiate = FALSE, outcome = "Death")

}
